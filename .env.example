# ElevenLabs API key used by apps/game's audio-admin dev-server plugin
# (packages/audio) to generate real AI voice/SFX takes. Leave unset to fall
# back to the zero-dependency Mock provider (no external calls, no key
# needed). Copy this file to .env and fill in a real key to use ElevenLabs.
ELEVENLABS_API_KEY=
